import { FiTrendingUp, FiXCircle } from 'react-icons/fi';
import { FaShoppingCart } from 'react-icons/fa';
import { properties } from '../data/platform';
import { SectionHeader } from '../components/ui';
function Marketplace(){return <div className="bg-page min-h-screen"><section className="container py-16"><SectionHeader eyebrow="Secondary market" title="Liquidity for tokenized property ownership" description="Users can list, buy, cancel and track property token trades instead of waiting for a full property exit."/><div className="grid lg:grid-cols-3 gap-8">{properties.map((p,i)=><div key={p.id} className="glass-card p-6"><img src={p.image} className="h-48 w-full rounded-3xl object-cover mb-5"/><h3 className="text-xl font-black">{p.tokenSymbol}</h3><p className="text-secondary-500 text-sm">{p.title}</p><div className="grid grid-cols-2 gap-3 my-5"><Box label="Floor" value={`$${(p.tokenPrice+(i+1)*.8).toFixed(2)}`}/><Box label="Last trade" value={`$${(p.tokenPrice+(i+1)*.55).toFixed(2)}`}/><Box label="Listed" value={`${1200+i*450}`}/><Box label="Fee" value="1.5%"/></div><div className="flex gap-3"><button className="btn flex-1 justify-center"><FaShoppingCart className="mr-2"/> Buy</button><button className="btn-secondary flex-1 justify-center"><FiXCircle className="mr-2"/> Cancel</button></div><p className="text-xs text-secondary-500 mt-4 flex gap-2"><FiTrendingUp/> Marketplace contract should lock listed tokens and atomically transfer payment + ownership.</p></div>)}</div></section></div>}
function Box({label,value}){return <div className="rounded-2xl bg-secondary-50 p-4"><p className="text-xs text-secondary-500">{label}</p><b>{value}</b></div>}
export default Marketplace;
